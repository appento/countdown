import 'package:flutter/material.dart';

/// A class that holds all the colors used in the app.
class ColorConstants {
  static const Color brightPinkCrayola = Color(0xFFEF476F);
  static const Color sunglow = Color(0xFF5e548e);
  static const Color emerald = Color(0xFF579063);
  static const Color blueNcs = Color(0xFF118AB2);
  static const Color midnightGreen = Color(0xFF5e6472);
}
